(function(){
		'use strict';

angular.module('nutGoApp')
  .factory('UtilService', ['$scope','$localStorage', '$cookieStore', '$http', function ($scope, $localStorage, $cookieStore, $http) {
      console.log("UtilService");

      var service = {

      };
      return service;
  }]);

  
})();
